# MASTER PROMPT — Initial Build Specification (v1)

> This is the initial master prompt used to construct the Accenture 2026 preparation repository. Retained in `_meta/` for build history and maintainer context.
